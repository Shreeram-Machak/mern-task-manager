# Frontend

React + Vite frontend for the MERN Task Manager.

## Scripts

```bash
npm run dev
npm run build
npm run lint
```

Set `VITE_API_URL` if the backend is not running at `http://localhost:5000/api`.
